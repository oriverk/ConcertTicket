<%= form_with(model: concert_detail, local: true) do |form| %>
  <% if concert_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert_detail.errors.count, "error") %> prohibited this concert_detail from being saved:</h2>

      <ul>
      <% concert_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :concert_id %>
    <%= form.text_field :concert_id %>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.number_field :grade %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.number_field :capacity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
