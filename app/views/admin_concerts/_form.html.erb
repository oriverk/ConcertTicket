<%= form_with(model: admin_concert, local: true) do |form| %>
  <% if admin_concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_concert.errors.count, "error") %> prohibited this admin_concert from being saved:</h2>

      <ul>
      <% admin_concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
